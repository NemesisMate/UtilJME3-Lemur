apply from: "../subBuild.gradle"

dependencies {
    compile "$jme_core"
    compile "$simsilica_lemur"
    compile "$simsilica_lemur_proto"
//    compile "com.simsilica:lemur-props:+"
    //compile "org.slf4:slf4-api"
//    compile 'ch.qos.logback:logback-core:+'
//    compile 'ch.qos.logback:logback-classic:+'

    compile "$guava"
    compile "$groovy"

    compile(get_module("UtilJME3-Base"))
}